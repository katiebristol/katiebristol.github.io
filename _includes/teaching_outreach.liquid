<div class="col">
  <a href="{% if teaching-outreach.redirect %}{{ teaching-outreach.redirect }}{% else %}{{ teaching-outreach.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if teaching-outreach.img %}
        {%
          include figure.liquid
          loading="eager"
          path=teaching-outreach.img
          sizes = "250px"
          alt="gallery thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ teaching-outreach.title }}</h2>
        <p class="card-text">{{ teaching-outreach.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if teaching-outreach.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ teaching-outreach.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if teaching-outreach.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ teaching-outreach.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
